<Window x:Class="learn_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:learn_wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding ChangePage}" CommandParameter="0">LoginPage</Button>
            <Button Command="{Binding ChangePage}" CommandParameter="1">HomePage</Button>
        </StackPanel>
        <Grid>
            <Frame x:Name="MainFrame" Content="{Binding CurrentPage, Converter={local:PageValueConverter}}" Background="Transparent" />
        </Grid>
    </StackPanel>
</Window>
